face_reco.py creates encodings for new faces and stores it in a text file
FaceRecognition.py runs face rec on the pre stored image and shows the result
once encodings are created the sample images are not required 
update.py is used to add more encodings to an existing facial data
